# Changelog

This project records user-visible changes by release tag.

## Unreleased

- Prepare the initial OSS publication documents and GitHub templates.
- Keep future entries focused on user-visible CLI flags, command output,
  AWS/Kubernetes resources, and cleanup behavior.

## <version> - YYYY-MM-DD

- Initial release of `local-irsa` for trying AWS IRSA-style WebIdentity
  authentication on local Kubernetes clusters.
- Include `init`, `install`, `bind`, `unbind`, `doctor`, `down`, and `demo`
  workflows.
